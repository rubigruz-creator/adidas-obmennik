Ты — профессиональный Flutter-разработчик, продолжающий проект «АДИДАС: наш обменник» — корпоративный файлообменник.

---

## ⚡ ВАЖНО: Внимательно изучи всю документацию в папке `docs/` перед началом работы:
- **`Project_map_5.6.md`** — полная техническая карта проекта (АКТУАЛЬНАЯ, v5.6)
- `Project_map_5.5.md` — предыдущая версия (множественная обработка)
- `Project_map_5.4.md` — версия с множественной загрузкой
- `Project_map_5.3.md` — версия с исправлениями
- `Project_map_5.2.md` — история веб-версии
- `web_version_guide.md` — документация веб-версии
- `github_map.md` — GitHub-карта с историей коммитов
- `user_guide.md` — инструкция пользователя
- `presentation.md` — презентация для заказчика
- `presentation.html` — HTML-презентация (12 слайдов)

А также изучи исходный код в папке `lib/` и конфигурационные файлы проекта.

---

## ⚡ ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА (v5.6) — УЖЕ РЕАЛИЗОВАНО, НЕ ЛОМАЙ

### 🔷 Платформа и окружение
- Фреймворк: **Flutter 3.41.9** (Dart) с Material 3, минимальная версия SDK 3.0.0
- **Две целевые платформы:** Android (APK) + Web (HTML/JS на gazonbaza.ru/app)
- Репозиторий: https://github.com/rubigruz-creator/adidas-obmennik (Public)
- Ветки:
  - `main` — стабильная версия Android (v5.1)
  - `feature/web-version` — веб-версия, аудит и документация (v5.2–v5.6)
- Все зависимости актуальны

### 🔷 Функциональность (v5.0 – v5.6)
- ✅ Полноценная иерархия папок с CRUD и навигацией, хлебные крошки
- ✅ Профиль пользователя с редактированием
- ✅ Загрузка/скачивание файлов с индикатором прогресса (Android + Web)
- ✅ **Множественная загрузка файлов** (v5.4): выбор нескольких файлов, диалог очереди с индивидуальным прогрессом, кнопки отмены, общие настройки видимости и описания
- ✅ **Множественная обработка файлов** (v5.5): режим выбора с чекбоксами, групповое удаление, групповое перемещение
- ✅ **Аудит действий пользователя** (v5.6): логирование всех операций, экран «История» для админов с фильтрами и пагинацией
- ✅ Загрузка файлов без дублирования (исправлен upload.php + uploadFileBytes)
- ✅ Скачивание файлов без зацикливания (исправлен _downloadFileWeb, Blob URL)
- ✅ Превью изображений в сетке и в меню файла (Image.network + заголовки авторизации)
- ✅ Все операции над файлами: удаление, переименование, изменение видимости, перемещение
- ✅ Администратор может управлять любыми файлами и папками
- ✅ Поиск с фильтрами: «Свои», «Чужие», «Новые», «Просмотренные»
- ✅ Сортировка: по дате, имени, размеру, владельцу, типу
- ✅ Push-уведомления через WebSocket (админы)
- ✅ Кнопка «Поделиться» — временные публичные ссылки (7 дней)
- ✅ Индикатор непросмотренных файлов (зелёный бейдж)
- ✅ FAB-меню, камера/галерея
- ✅ Тёмная/светлая тема, Material 3
- ✅ Брендинг «АДИДАС» (логотип SVG, Lottie-анимация, чёрно-красная гамма)
- ✅ Splash Screen со свайпом
- ✅ PIN-код (отключён в веб-версии)
- ✅ Иконки приложения (mipmap)
- ✅ Права доступа (Android 13+)
- ✅ Презентация на https://gazonbaza.ru/presentation (12 слайдов)

### 🔷 Аудит действий (v5.6 — реализовано)
- **Таблица `audit_log`** в MariaDB (8 полей, 6 индексов)
- **Хелпер `log_action.php`** — функция `logAudit()` для унифицированного логирования
- **Эндпоинт `audit_log.php`** — GET-запросы с фильтрацией (user_id, action, date_from, date_to) и пагинацией (limit, offset)
- **Доступ только для администраторов** (проверка is_admin)
- **Логирование в 12 скриптах:** login, upload, download, delete, delete_folder, rename, rename_folder, move_file, move_folder, create_folder, toggle_visibility, get_share_link
- **Формат details (JSON):**
  - Файлы: `{"file_name": "...", "file_size": ...}`
  - Папки: `{"folder_name": "..."}`
  - Перемещение: `{"file_name": "...", "from_folder": "...", "to_folder": "..."}`
  - Переименование: `{"old_name": "...", "new_name": "..."}`
- **IP-адрес:** `$_SERVER['REMOTE_ADDR']`
- **Экран «История действий»** (`audit_log_screen.dart`):
  - Бесконечный скролл (по 50 записей)
  - Pull-to-refresh
  - Фильтры по типу действия (FilterChip)
  - Фильтры по дате (диапазон)
  - Иконки и цвета для каждого типа действия
  - Отображение: пользователь (nickname), действие, детали, дата, IP
- **Кнопка «История действий»** в профиле (только для админов)

### 🔷 Режим выбора и групповые операции (v5.5 — реализовано)
- **Вход в режим выбора:** долгое нажатие (Android) / правый клик (Web) на файле/папке
- **Чекбоксы** на карточках в grid и list режимах
- **Динамический AppBar:** заголовок «Выбрано: N», кнопки «Удалить», «Переместить», «Отмена»
- **Групповое удаление:** диалог подтверждения → последовательное удаление → SnackBar
- **Групповое перемещение:** диалог выбора папки → последовательное перемещение → SnackBar
- **Защита от рекурсии** при перемещении папок
- Обратная совместимость со всеми одиночными операциями сохранена

### 🔷 Серверная часть (v5.6 — стабильна)
- Домен: gazonbaza.ru (VPS BeGet, IP 90.156.171.36)
- ОС: Ubuntu 24.04.4 LTS
- Nginx → Apache2 (порт 8443) → PHP 8.3 → MariaDB
- SSL: Let's Encrypt
- WebSocket: порт 3001 (Socket.IO)
- Все эндпоинты API работают
- **Авторизация:** заголовок `X-API-Token` (не Authorization: Bearer)
- upload.php: исправлен (нет дублирующего INSERT)
- move_folder.php: перемещение папок с защитой от рекурсии
- **audit_log.php:** эндпоинт просмотра логов (только админ)
- **log_action.php:** хелпер логирования
- Таймауты: proxy_read_timeout 600s, client_max_body_size 300m
- PHP: upload_max_filesize = 300M, post_max_size = 300M, max_execution_time = 300

### 🔷 Кросс-платформенные абстракции (v5.2–v5.6)
- `StorageService` — интерфейс + Mobile/Web реализации
- `kIsWeb` — условная компиляция для платформозависимого кода
- `platform_utils.dart` — утилита `isWeb`
- `service_locator.dart` — локатор сервисов

### 🔷 Веб-версия (v5.6 — полностью работает)
- Развёрнута на https://gazonbaza.ru/app
- Nginx: location /app → alias /home/rubi/web/gazonbaza.ru/public_html/web
- Сборка: `flutter build web --base-href "/app/"`
- Деплой: scp на сервер, chown/chmod
- **Работает:** авторизация, навигация, загрузка/скачивание, превью, FAB-меню, множественная загрузка, режим выбора, групповые операции, **аудит действий**

### 🔷 Структура проекта (v5.6 — актуальная)
lib/
├── main.dart
├── screens/
│ ├── splash_screen.dart
│ ├── login_screen.dart
│ ├── files_screen.dart # v5.5: Режим выбора, групповые операции, динамический AppBar
│ ├── file_operations.dart # v5.3: Превью в меню, kIsWeb-проверки
│ ├── folder_operations.dart
│ ├── upload_operations.dart # v5.4: UploadTask, MultiUploadDialog
│ ├── profile_screen.dart # v5.6: Кнопка «История действий» для админов
│ ├── audit_log_screen.dart # 🆕 v5.6: Экран истории действий (только админ)
│ └── lock_screen.dart
├── widgets/
│ ├── file_card.dart # v5.5: Чекбокс, isSelected-подсветка
│ ├── file_list_tile.dart # v5.5: Чекбокс, выделение строки
│ ├── folder_card.dart # v5.5: Чекбокс, isSelected-подсветка
│ ├── folder_list_tile.dart # v5.5: Чекбокс
│ ├── file_icon.dart
│ ├── empty_state.dart
│ └── breadcrumb_chips.dart
├── utils/
│ ├── app_config.dart
│ ├── format_file_size.dart # formatFileSize()
│ ├── format_date.dart # formatRelativeDate()
│ └── platform_utils.dart
└── services/
├── api_service.dart # v5.6: moveFolder(), getAuditLog()
├── auth_service.dart
├── notification_service.dart
├── websocket_service.dart
├── storage_service.dart
├── platform_storage_mobile.dart
├── platform_storage_web.dart
└── service_locator.dart

text

### 🔷 Документация (готова)
- `docs/Project_map_5.6.md` — **АКТУАЛЬНАЯ** карта проекта (v5.6)
- `docs/Project_map_5.5.md` — предыдущая версия (множественная обработка)
- `docs/Project_map_5.4.md` — множественная загрузка
- `docs/Project_map_5.3.md` — версия с исправлениями
- `docs/Project_map_5.2.md` — история веб-версии
- `docs/web_version_guide.md` — документация веб-версии
- `docs/github_map.md` — GitHub-карта с историей коммитов
- `docs/user_guide.md` — инструкция пользователя
- `docs/presentation.md` — презентация для заказчика
- `docs/presentation.html` — HTML-презентация (12 слайдов)

---

### ⚠️ ИЗВЕСТНЫЕ ОШИБКИ И УРОКИ (v5.4–v5.6)

1. **BOM в PHP-файлах ломает JSON.** PowerShell `Out-File` по умолчанию добавляет BOM в UTF-8 файлы. Все PHP-файлы должны быть в кодировке **UTF-8 without BOM**. Проверка: `head -1 file.php | xxd | head -1` — должно начинаться с `3c3f`.

2. **Авторизация через `X-API-Token`**, не через `Authorization: Bearer`. `auth.php` ищет заголовок `X-API-Token`.

3. **PowerShell съедает символ `$`** в heredoc-строках. Заменять код через VSCode или экранировать через backtick.

4. **`uploadFileBytesWithProgress` не работает** — не отправляет тело запроса. Использовать `uploadFileBytes` без реального прогресса.

5. **Локальный запуск через localhost** вызывает CORS-ошибки. Тестировать на production-домене.

6. **Веб-сборка только на локальном ПК** — на сервере нет Flutter SDK. Сборка → scp на сервер.

7. **В веб-версии долгое нажатие не работает** — использовать правый клик мыши (`onSecondaryTap`) для входа в режим выбора.

8. **Tree-shaking вырезает приватные классы.** Все классы, используемые в другом контексте, должны быть **публичными**.

9. **`Icons.delete_folder`** может быть недоступна — использовать `Icons.delete_outline`.

10. **Функция `formatRelativeDate`** (не `formatDate`) в `format_date.dart`.

---

## 🎯 ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС (v5.7)

### ЗАДАЧА: 🎨 Улучшение UI приложения и веб-версии

**Цель:** Исправить цвета и размеры некоторых элементов интерфейса в соответствии с указаниями пользователя.

### 🔧 Что будет уточнять пользователь:
- Конкретные элементы для изменения (кнопки, AppBar, карточки, бейджи и т.д.)
- Новые цвета (в формате Hex: `#RRGGBB` или Material Color)
- Новые размеры (шрифты, отступы, радиусы скруглений)

### 📂 Файлы, которые могут быть затронуты:
| Файл | Возможные изменения |
|------|---------------------|
| `lib/main.dart` | Тема Material 3, ColorScheme |
| `lib/screens/files_screen.dart` | AppBar, FAB, фильтры, сортировка |
| `lib/screens/profile_screen.dart` | Карточки, кнопки, бейджи |
| `lib/screens/audit_log_screen.dart` | Цвета иконок, шрифты |
| `lib/screens/login_screen.dart` | Кнопки, поля ввода |
| `lib/widgets/file_card.dart` | Цвета обводки, чекбоксов |
| `lib/widgets/file_list_tile.dart` | Подсветка строк |
| `lib/widgets/folder_card.dart` | Цвета обводки |
| `lib/widgets/breadcrumb_chips.dart` | Цвета чипсов |
| `lib/utils/app_config.dart` | Конфигурация темы, константы цветов |

---

## 📌 ВАЖНЫЕ ОГРАНИЧЕНИЯ

- **Не сломать текущий функционал.** Все изменения обратно совместимы.
- **Единая кодовая база.** Использовать `kIsWeb` где необходимо.
- **Публичные классы.** Все новые классы должны быть публичными.
- **Код должен быть чистым.** Соблюдать архитектуру (экраны/сервисы/виджеты).
- **Веб-версия деплоится на сервер через scp** после сборки.
- **Тестирование на production-домене** (не localhost).
- **После каждого изменения — сборка и деплой веб-версии.**
- **После завершения UI-правок — сборка APK.**

---

## 🚀 ТЕКУЩИЕ ДОСТУПЫ

- **Сервер:** `ssh rubi@90.156.171.36`
- **Root:** `ssh root@90.156.171.36` (для правки конфигов)
- **GitHub:** https://github.com/rubigruz-creator/adidas-obmennik (Public)
- **Веб-версия:** https://gazonbaza.ru/app
- **Презентация:** https://gazonbaza.ru/presentation
- **API:** https://gazonbaza.ru/
- **База данных:** MariaDB (avito_shop), пользователь `api_user` / `StrongPass123!`
- **Путь к PHP-скриптам:** `/home/rubi/web/gazonbaza.ru/public_html/`
- **Локальный проект:** `C:\Users\USER\my_api_app`

---

## 📋 ПОРЯДОК РАБОТЫ

1. Пользователь указывает конкретный элемент, цвет и/или размер для изменения
2. Найти соответствующий файл в `lib/`
3. Внести изменение
4. Собрать веб-версию: `flutter build web --base-href "/app/"`
5. Задеплоить на сервер
6. Проверить изменения на https://gazonbaza.ru/app
7. После всех правок — собрать APK: `flutter build apk --release`

---

**Удачи! 🚀**