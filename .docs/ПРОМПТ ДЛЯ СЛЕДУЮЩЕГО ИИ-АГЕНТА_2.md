Ты — профессиональный разработчик, продолжающий проект "Кусочница" — корпоративный файлообменник.

Внимательно прочитай файл документации Project_map.md (приложен).

## ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА

### СЕРВЕР (Ubuntu + HestiaCP + Nginx/Apache + PHP 8.1 + MariaDB)
- Домен: gazonbaza.ru (IP: 90.156.171.36)
- Путь к проекту: /home/rubi/web/gazonbaza.ru/public_html/
- Хранилище файлов: /home/rubi/web/gazonbaza.ru/public_html/files/
- База данных: avito_shop (пользователь: api_user, пароль: StrongPass123!)

### API ENDPOINTS (все работают)
✅ register.php — регистрация
✅ login.php — вход, выдача токена X-API-Token
✅ list_files.php — список файлов с поддержкой поиска (?search=...)
✅ upload.php — загрузка файлов (до 300 МБ) с параметром is_public (0/1)
✅ download.php — скачивание файлов
✅ delete.php — удаление (админ любые, user только свои)
✅ rename.php — переименование файла (админ или владелец)
✅ toggle_visibility.php — переключение видимости файла (общий/личный)
✅ auth.php — проверка токена (возвращает id, phone, nickname, full_name, position, is_admin)

### FLUTTER-ПРИЛОЖЕНИЕ
- Использует IP-адрес (90.156.171.36) с заголовком Host: gazonbaza.ru (обход проблем DNS эмулятора)
- SSL-сертификат обходится через HttpOverrides в main.dart (только для разработки)
- Структура: screens/ (login_screen, files_screen), widgets/ (file_pill), services/ (api_service, auth_service)
- Реализованный функционал:
  ✅ Регистрация и вход с сохранением токена
  ✅ Загрузка любых файлов, фото из галереи и с камеры
  ✅ Выбор "Общий" / "Личный" при каждой загрузке
  ✅ Скачивание файлов в папку Downloads
  ✅ Удаление файлов (свои для всех, любые для админа)
  ✅ Переименование файлов через диалог (владелец или админ)
  ✅ Переключение видимости файла прямо из меню (тап по индикатору)
  ✅ Поиск файлов по имени (строка поиска в AppBar)
  ✅ Цветные пилюли файлов с сортировкой (новые крупнее)

### БАЗА ДАННЫХ
Таблицы:
- users (id, phone, password_hash, nickname, full_name, position, is_admin, created_at)
- user_sessions (id, user_id, api_token, expires_at, created_at)
- files (id, user_id, original_name, stored_name, file_size, file_type, is_public, upload_date)
- api_keys (устаревшая, не используется)

### ТЕСТОВЫЕ ДАННЫЕ
- Администратор: телефон 111111, пароль 111111, is_admin = 1
- Обычный пользователь: телефон +79001234567, пароль задан при регистрации

## ЗАДАЧИ ДЛЯ ПРОДОЛЖЕНИЯ (в порядке приоритета)

1. СДЕЛАТЬ ПАПКИ И КАТЕГОРИИ (самое сложное)
   - Создать таблицу `folders` (id, user_id, name, parent_id, created_at)
   - Добавить `folder_id` в таблицу `files`
   - Реализовать API для создания/удаления папок, перемещения файлов
   - В приложении отображать дерево папок, возможность навигации
   - Поддержка вложенности, drag-and-drop (опционально)

2. ДОБАВИТЬ РЕДАКТИРОВАНИЕ ПРОФИЛЯ ПОЛЬЗОВАТЕЛЯ
   - Создать profile.php (GET/POST) для получения и обновления nickname, full_name, position
   - В приложении добавить экран профиля с возможностью редактирования
   - Проверка уникальности nickname (если требуется)
   - Админ может редактировать чужие профили (опционально)

3. СДЕЛАТЬ ПРОГРЕСС-БАР ПРИ ЗАГРУЗКЕ/СКАЧИВАНИИ
   - Для загрузки: отслеживать прогресс отправки файла через MultipartRequest
   - Для скачивания: использовать StreamedResponse и считать загруженные байты
   - Отображать прогресс в виде линейного индикатора или SnackBar с процентом
   - Возможность отмены загрузки/скачивания

4. ДОБАВИТЬ PUSH-УВЕДОМЛЕНИЯ О НОВЫХ ФАЙЛАХ
   - Серверная часть: при загрузке файла отправлять уведомление через Firebase Cloud Messaging (FCM) или сохранять токены устройств
   - Регистрация устройства для push-уведомлений
   - Клиентская часть: принимать уведомления, показывать в шторке, открывать приложение

## ТРЕБОВАНИЯ К ТВОЕЙ РАБОТЕ
- Пиши код, который сразу работает (без лишних зависимостей)
- Для PHP: всегда проверяй права (админ vs владелец), используй подготовленные выражения PDO
- Для Flutter: используй существующие сервисы (ApiService, AuthService) и виджеты
- После каждого изменения давай команды для проверки (curl для серверной части, flutter run для клиента)
- Перед тем как менять работающие файлы, запроси их содержимое для анализа
- Сохраняй обратную совместимость: уже работающий функционал не должен ломаться

## ПЕРВАЯ ЗАДАЧА (рекомендую начать с самой сложной):
Реализуй ПАПКИ И КАТЕГОРИИ. Это потребует изменений в БД, новых PHP-эндпоинтов, доработки list_files.php, upload.php, а также значительного обновления Flutter-интерфейса. Тщательно спланируй структуру и предложи пользователю подтверждение перед кодированием.

Начинай!