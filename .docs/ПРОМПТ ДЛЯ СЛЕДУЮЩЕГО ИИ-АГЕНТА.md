ПРОМПТ ДЛЯ СЛЕДУЮЩЕГО ИИ-АГЕНТА



Ты — профессиональный разработчик, продолжающий проект "Кусочница" — корпоративный файлообменник.

Внимательно прочитай файл документации во вложении.

## ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА

### СЕРВЕР (Ubuntu + HestiaCP + Nginx/Apache + PHP 8.1 + MariaDB)
- Домен: gazonbaza.ru (IP: 90.156.171.36)
- Путь к проекту: /home/rubi/web/gazonbaza.ru/public_html/
- Хранилище файлов: /home/rubi/web/gazonbaza.ru/public_html/files/
- База данных: avito_shop (пользователь: api_user, пароль: StrongPass123!)

### API ENDPOINTS (все работают)
✅ register.php — регистрация
✅ login.php — вход, выдача токена X-API-Token
✅ list_files.php — список файлов
✅ upload.php — загрузка файлов (до 300 МБ)
✅ download.php — скачивание файлов
✅ delete.php — удаление (админ любыe, user только свои)
✅ auth.php — проверка токена

### FLUTTER-ПРИЛОЖЕНИЕ
- Структура: screens/ (login_screen, files_screen), widgets/, services/
- Функционал: вход/регистрация, загрузка(файлы/фото/камера), скачивание, удаление
- Сборка APK: flutter build apk --release

### БАЗА ДАННЫХ
Таблицы: users (is_admin), user_sessions, files, api_keys (устаревшая)

### АДМИН
Телефон: 111111, пароль: password, is_admin: 1

## ЗАДАЧИ ДЛЯ ПРОДОЛЖЕНИЯ (выбери приоритет):

1. ДОБАВИТЬ ПЕРЕИМЕНОВАНИЕ ФАЙЛОВ (rename.php + кнопка в меню)
2. СДЕЛАТЬ ВЫБОР "ОБЩИЙ/ЛИЧНЫЙ" ФАЙЛ ПРИ ЗАГРУЗКЕ
3. ДОБАВИТЬ ПОИСК ПО ФАЙЛАМ (search.php + строка поиска)
4. СДЕЛАТЬ ПАПКИ И КАТЕГОРИИ (самое сложное)
5. ДОБАВИТЬ РЕДАКТИРОВАНИЕ ПРОФИЛЯ ПОЛЬЗОВАТЕЛЯ
6. СДЕЛАТЬ ПРОГРЕСС-БАР ПРИ ЗАГРУЗКЕ/СКАЧИВАНИИ
7. ДОБАВИТЬ PUSH-УВЕДОМЛЕНИЯ О НОВЫХ ФАЙЛАХ

## ТРЕБОВАНИЯ К ТВОЕЙ РАБОТЕ:
- Пиши код, который сразу работает (без лишних зависимостей)
- Для PHP: добавляй проверку прав (админ vs обычный user)
- Для Flutter: используй существующие сервисы (api_service, auth_service)
- После каждого изменения давай команды для проверки (curl для PHP, flutter run для клиента)
- перед тем как первый раз менять работающие файлы - запроси их на анализ.

## ПЕРВАЯ ЗАДАЧА (рекомендую):
Реализуй ПЕРЕИМЕНОВАНИЕ ФАЙЛОВ:
- Создай rename.php на сервере (метод POST, принимает id и new_name)
- Проверка прав: админ или владелец
- Обновление original_name в БД
- В files_screen.dart добавь кнопку "Переименовать" (можно через диалог)
- Вызови ApiService.renameFile(token, fileId, newName)

Начинай!